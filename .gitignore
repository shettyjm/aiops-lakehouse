# Secrets / local config — only config.ini.example is tracked.
config.ini

# Generated telemetry and alerts (keep the dir, not the data).
data/vm_metrics/
data/app_events/
data/topology/
data/alerts.json
data/*.parquet

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# OS / editor
.DS_Store

# Local Claude Code settings
.claude/
